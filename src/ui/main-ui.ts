import { Settings } from '../settings';

// This file is kept as an empty placeholder for future UI expansion
// but doesn't need any actual code for now

export function createMainUI() {
  // No custom UI needed
}

export function removeMainUI() {
  // No custom UI to remove
}